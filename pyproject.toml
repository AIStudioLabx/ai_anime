[project]
name = "ai_anime"
version = "0.1.0"
description = "AI 漫剧生成项目"
requires-python = ">=3.11.8"
dependencies = [
    "requests>=2.31.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["comfy", "scripts"]
